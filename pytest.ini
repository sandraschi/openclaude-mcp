# pytest.ini is intentionally minimal — all config lives in pyproject.toml [tool.pytest.ini_options]
# This file exists only to prevent pytest from walking up the directory tree looking for config.
